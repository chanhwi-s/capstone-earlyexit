# Work Log — 2026-05-05

## 완료 작업

### 1. bs2별 avg latency vs plain 비교 플롯 추가 (`hybrid_vit_2exit_goodput.py`)
- `plot_avg_latency_vs_batchsize()` 추가
- 좌: 전체 샘플(exit1+exit2) avg latency 바 차트 + plain 점선
- 우: hybrid/plain latency ratio 바 차트
- 출력: `avg_latency_vs_batchsize.png`

### 2. Latency distribution (bimodal) 시각화 → 이후 제거
- `src/analysis/plot_latency_dist.py` 생성 (후에 삭제됨)
- exit1(빠른 경로)/exit2(느린 경로) KDE 분리 → bimodal vs plain unimodal 대비
- goodput 벤치마크 실행 시 자동 생성되도록 통합했다가 요청으로 제거

### 3. Threshold별 accuracy sweep (`src/benchmark/sweep_accuracy_vs_threshold.py`)
- 목적: earlyexit으로 인한 accuracy overhead 정량화
- 기준선: "all-exit2" (모든 샘플 seg2 통과) = finetuned 모델 최고 accuracy
- GPU 1회 패스로 (logit1, logit2) 동시 수집 → threshold 후처리 sweep
- 출력: `accuracy_vs_threshold.png`, `accuracy_sweep.csv/json`
- `static_benchmark.sh` Step 4로 통합 (SKIP_ACC_SWEEP=1로 스킵 가능)

### 4. Dynamic batching 벤치마크 신규 추가
- `src/export/export_onnx_seg2_dynamic.py`: seg2 dynamic batch ONNX export
- `src/benchmark/dynamic_benchmark.py`: timeout_ms sweep 시뮬레이션
  - flush 조건: `len(queue) >= max_bs2` OR `t - queue[0].ts_seg1_done >= timeout_ms`
  - static과 달리 end-of-run partial queue 강제 flush (폐기 없음)
  - SLO 하나 고정, timeout 값 목록 sweep
  - 출력: `dynamic_goodput_vs_timeout.png` (좌: goodput, 우: avg latency)
- `scripts/dynamic_benchmark.sh`: 3단계 체인 (seg1+plain export → seg2_dynamic export → sweep)

### 5. 스크립트 이름 정리
- `benchmark_hybrid_2exit_goodput_large_5090.sh` → `scripts/static_benchmark.sh`
- 신규: `scripts/dynamic_benchmark.sh`

## 실행 방법
```bash
# static 벤치마크 (기존)
nohup bash scripts/static_benchmark.sh --threshold 0.70 > static.log 2>&1 &

# dynamic batching sweep (신규)
nohup bash scripts/dynamic_benchmark.sh --threshold 0.70 > dynamic.log 2>&1 &

# SLO/timeout 조정
SLO_MS=150 TIMEOUT_VALUES="1 5 10 50 200" bash scripts/dynamic_benchmark.sh --threshold 0.70
```

## 주요 파일 목록
| 파일 | 역할 |
|------|------|
| `scripts/static_benchmark.sh` | static batching 벤치마크 (4단계) |
| `scripts/dynamic_benchmark.sh` | dynamic batching timeout sweep |
| `src/benchmark/hybrid_vit_2exit_goodput.py` | static 벤치마크 코어 |
| `src/benchmark/dynamic_benchmark.py` | dynamic 벤치마크 코어 |
| `src/benchmark/sweep_accuracy_vs_threshold.py` | threshold별 accuracy 분석 |
| `src/export/export_onnx_seg2_dynamic.py` | seg2 dynamic ONNX export |
