build:
  devices: [gpu, dla]
  precisions: [fp32, fp16, int8]

  dla_core: 9
  allow_gpu_fallback: true

  workspace: 2048
  opt_level: 5
  use_cuda_graph: true

run:
  iterations: 200
  warmup: 20
  streams: 1
